import type { AttemptQuestion } from "@/lib/api/exam-attempt-client";
export function QuestionNavigator({ questions, answers, currentIndex, onSelect }: { questions: AttemptQuestion[]; answers: Record<string, { selectedOptionIds?: string[]; statementAnswers?: Array<{ statementId: string; answer: boolean }>; isFlagged: boolean }>; currentIndex: number; onSelect: (index: number) => void }) { return <aside className="question-navigator"><h2>Question list</h2><div className="navigator-grid">{questions.map((question, index) => { const answer = answers[question.questionId]; const answered = Boolean(answer?.selectedOptionIds?.length || answer?.statementAnswers?.length); return <button key={question.questionId} type="button" className={`${index === currentIndex ? "current " : ""}${answered ? "answered " : ""}${answer?.isFlagged ? "flagged" : ""}`} aria-label={`Question ${index + 1}${answered ? ", answered" : ", unanswered"}${answer?.isFlagged ? ", flagged" : ""}`} onClick={() => onSelect(index)}>{index + 1}</button>; })}</div><div className="navigator-legend"><span>● Answered</span><span>⚑ Flagged</span><span>○ Unanswered</span></div></aside>; }
