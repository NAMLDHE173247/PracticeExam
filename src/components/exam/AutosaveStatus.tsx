export function AutosaveStatus({ state, hasPending }: { state: "idle" | "saving" | "saved" | "failed"; hasPending: boolean }) { const label = state === "saving" || hasPending ? "Saving…" : state === "saved" ? "Saved" : state === "failed" ? "Save failed — retry by changing the answer" : "Changes save automatically"; return <span className={`autosave-status ${state === "failed" ? "failed" : ""}`} aria-live="polite">{label}</span>; }
